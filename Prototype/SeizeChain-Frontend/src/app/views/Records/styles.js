import { makeStyles } from "@material-ui/core";

const styles = makeStyles((theme) => ({
	root: {
		height: "100vh",
		width: "100%",
	},

	contentContainer: {
		width: "100%",
		padding: "5%",
	},
}));

export default styles;
