import { makeStyles } from "@material-ui/core";

const styles = makeStyles((theme) => ({
	root: {
		width: "100%",
		minHeight: "100vh",
	},

	contentContainer: {
		width: "100%",
		padding: "5%",
	},

	gridroot: {
		width: "100%",
		flexGrow: 1,
	},
}));

export default styles;
