import { makeStyles } from "@material-ui/core";

const styles = makeStyles((theme) => ({
	root: {
		width: "100%",
		minHeight: "100vh",
	},

	contentContainer: {
		width: "100%",
		padding: "5%",
	},
}));

export default styles;
